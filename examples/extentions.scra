(declare
	(name "Extentions Example")
)
(when flag
	(say (join "Video Motion on Sprite: " (video motion sprite) ) )
	(say (join "Boost Motor A Position: " (boost_motorpos A) ) )
	(ask "What tilt direction?")
	(say (join "Boost tilt: " (boost_tilt (value answer) ) ) )
)
